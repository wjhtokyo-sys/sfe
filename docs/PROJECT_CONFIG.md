# SFE 项目配置

## 货币配置

- 系统默认币种：`JPY`
- 显示货币：日元（円）
- 适用范围：账单默认币种、账单相关金额业务

## 说明

- 旧历史数据若已是其他币种（如 CNY）会按历史值保留，不自动改写。
- 新生成账单默认使用 JPY。

## 时间配置

- 系统新增业务时间默认与本地系统时间同步（`datetime.now`）。
- 适用范围：通用 `created_at` 默认时间、账单状态推进时间、归档时间等新增写入时间。
